<!DOCTYPE html>
<html lang="en">
    <head>
        <title></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="css/style.css" rel="stylesheet">
    </head>
    <body>
        <% for (professor of professoresDisponiveis) { %>
            <span><%= professor.nomeProfessor %></span>
            <span><%= professor.id_professor %></span>
            <a href="/chatAluno/<%= professor.id_professor %>"> Chat -> </a> <br>
        <% } %>
    </body>
</html>